# AI4SE PaperTracker 课题组使用手册

自动跟踪「AI 赋能软件工程（AI4SE）」论文的课题组工具：每日从 arXiv 拉取最新论文，经 DBLP 判定 CCF-A 会议（ICSE / FSE / ASE / ISSTA）发表情况，用「关键词 + DeepSeek LLM」识别 AI4SE 论文、打主题标签、生成中文摘要与亮点速读，并统计被引次数。本手册涵盖**全部功能**的使用说明与管理员部署指南。

---

## 目录

1. [快速开始](#1-快速开始)
2. [论文列表与筛选](#2-论文列表与筛选)
3. [论文详情](#3-论文详情)
4. [个性化阅读](#4-个性化阅读)
5. [数据分析与趋势](#5-数据分析与趋势)
6. [数据导出](#6-数据导出)
7. [账号与个人画像](#7-账号与个人画像)
8. [周报推送](#8-周报推送)
9. [管理员指南](#9-管理员指南)
10. [常见问题](#10-常见问题)

---

## 1. 快速开始

| 角色 | 操作 |
|---|---|
| **组员** | 浏览器打开 **<http://8.166.134.107/>**（公网地址，已部署服务器）→ 点「注册」创建账号 → 登录使用 |
| **管理员** | 服务已部署在阿里云服务器并自动运行，无需日常操作（详见[管理员指南](#9-管理员指南)） |

> **数据说明（重要）**：论文数据由管理员服务器**每日自动爬取更新**（arXiv 新论文 / DBLP 匹配 / LLM 分类 / 引用数），**使用者打开网页即看到全部共享数据，不需要也不应该执行任何爬取、初始化或回填操作**——那些是服务器管理员的维护命令（见[第 9 章](#9-管理员指南)）。
>
> 系统要求登录后才能使用（未登录访问任何页面会跳转登录页）。登录后所有个人数据（收藏/已读/稍后读/画像）独立保存，组员之间互不可见。

---

## 2. 论文列表与筛选

论文列表页（首页）是全站核心入口，左侧筛选栏 + 顶部工具栏 + 表格：

### 2.1 搜索与排序

- **关键词搜索**：顶部搜索框，回车或点「搜索」生效
- **高级筛选**（「高级筛选」按钮）：
  - 搜索范围：标题+摘要 / 仅标题 / 仅摘要
  - 作者：按姓名模糊匹配（也可从详情页/作者榜点击直达）
  - 年份区间：起止年份
  - 最低被引：只看被引 ≥ N 的论文
- **排序**（左侧栏）：
  - 最新发布（默认）
  - A 会优先（正式发表版在前）
  - 被引最多（🔥 引用数降序）

### 2.2 筛选条件（左侧栏）

| 条件 | 说明 |
|---|---|
| AI4SE 开关 | 仅看已确认的 AI4SE 论文 |
| 主题 | 10 类（代码生成/修复/翻译/摘要、缺陷检测、自动化测试、软件分析、需求工程、LLM4SE 通用、其他），显示各主题论文数 |
| 会议 | 四大 A 会（ICSE / FSE / ASE / ISSTA），显示匹配论文数 |
| 年份 | 按年份过滤（选项取自数据，动态生成） |
| 阅读状态 | 全部 / 只看收藏 ⭐ / 只看稍后读 / 只看未读 |

### 2.3 表格列

| 列 | 说明 |
|---|---|
| ☑ 勾选框 | 勾选多篇后可用「导出选中」批量导出（悬停有提示） |
| ⭐ 收藏星标 | 点击收藏/取消（需登录） |
| 标题 | 点击进入详情 |
| 作者 | 点击任意作者 → 筛选该作者全部论文 |
| 会议 / 年份 | A 会论文显示会议标签 |
| 被引 🔥 | 被引次数（Crossref / Semantic Scholar 双源统计，悬停有说明） |
| 主题 | 多主题彩色标签 |

- **已读淡化**：标记「已读」的论文行会半透明显示，配合「只看未读」快速过滤
- 分页：每页 10 / 20 / 50 条切换

---

## 3. 论文详情

点击列表任意论文进入详情页，自上而下：

| 区块 | 内容 |
|---|---|
| 元信息 | 会议 / 年份 / 被引 🔥 / 发布时间 / 处理状态 / A 会匹配状态 |
| 阅读标记 | ⭐ 收藏 / ✓ 已读 / 📌 稍后读（悬停有功能说明） |
| 主题标签 | 多主题彩色标签 |
| **亮点速读**（LLM） | 一句话「贡献」+「局限」，快速判断相关性 |
| **AI 深度摘要**（LLM） | 点「生成深度摘要」→ 结构化的背景 / 要解决的问题 / 方法 / 主要实验 / 结论（生成后缓存，重复打开零成本） |
| 中文摘要（LLM） | 中文解读 |
| 英文摘要 | arXiv 原文摘要 |
| 链接 | arXiv 摘要页 / **PDF 直达** / **复制 BibTeX**（一键复制引用格式）/ DBLP 收录页 / DOI |
| 作者 | 点击任意作者 → 查看该作者全部论文 |
| **相关论文推荐** | 同主题 + 标题相似论文，点击直接跳转 |

> 用途：打开论文 → 看亮点速读 → 必要时生成深度摘要 → 判断与课题是否相关，不合适直接跳过，节省通读时间。

---

## 4. 个性化阅读

登录后可用，全部按账号隔离：

- **⭐ 收藏**：重点论文，集中到「个人画像」回顾
- **✓ 已读**：读过的论文，列表行淡化 + 「只看未读」筛选
- **📌 稍后读**：待读清单，列表「阅读状态」筛选
- 组合用法：先「稍后读」囤一批 → 读完标记「已读」→ 用「只看未读」管理剩余

---

## 5. 数据分析与趋势

趋势页（侧栏「主题趋势」）共 5 个页签：

| 页签 | 内容 |
|---|---|
| 主题趋势 | 各主题论文数随时间折线（按天/周/月聚合切换）；**点击图例系列名 = 只看该主题，再击 = 恢复全部** |
| 会议趋势 | 各会议论文数随时间折线，同样支持图例交互 |
| 年份分布 | 历年论文总量 |
| 会议×主题热力 | 颜色深浅 = 论文数，快速定位「哪个会议更关注哪个主题」 |
| 作者榜 | 活跃学者 TOP 榜（论文数 / AI4SE 论文数 / 主要主题），**点击学者名 → 查看其全部论文** |

---

## 6. 数据导出

列表页工具栏两个导出入口，均支持 **CSV（Excel 直接打开）/ JSON / BibTeX** 三种格式：

| 入口 | 用途 |
|---|---|
| 导出筛选结果 | 导出符合**当前全部筛选条件**的论文（不勾选也有效） |
| 导出选中 (N) | 导出**勾选**的论文（先勾选左侧 ☑ 框） |

- CSV：UTF-8 BOM，Excel 打开不乱码；含被引数、收藏状态等列
- BibTeX：自动按论文类型生成 `@inproceedings` / `@article` / `@misc`（无正式版时 arXiv 预印本兜底），写综述/论文引用直接可用

---

## 7. 账号与个人画像

- **注册 / 登录**：用户名（或邮箱）+ 密码；密码 pbkdf2 加密存储
- **修改资料**：画像页「修改资料」→ 改用户名 / 邮箱
- **修改密码**：画像页「修改密码」→ 验证旧密码后设置新密码
- **个人画像页**（侧栏「个人画像」）：
  - 统计卡片：收藏数 / 已读数 / 稍后读数
  - 收藏论文主题分布（可视化条）
  - 最近收藏列表（点击直达详情）

---

## 8. 周报推送

每周五 09:30 自动生成（保存在服务器 `data/reports/`）：

| 周报 | 内容 |
|---|---|
| 系统周报 | 本周新增论文数 / AI4SE 占比 / 主题分布 / A 会新匹配 / 亮点论文 Top 10 |
| 个人周报 | 每个有收藏的用户：本周新收藏 + 收藏主题分布（`data/reports/personal/用户名-周.md`） |

---

## 9. 管理员指南

### 9.1 服务状态

系统已部署在阿里云服务器（<http://8.166.134.107/>），**systemd 托管、开机自启、崩溃自动重启，正常情况下无需任何操作**。管理员通过 SSH 远程管理：

```bash
ssh root@8.166.134.107
systemctl status papertracker    # 查看服务状态
systemctl restart papertracker   # 重启服务（更新后）
journalctl -u papertracker -n 50 # 查看最近日志
```

- 代码位置：`/opt/ai4se-papertracker/`（backend + frontend/dist + data）
- 引用数回填日志：`tail -f /root/citations.log`
- 本地开发调试仍可用根目录 `start.bat`（开发模式起后端 8000 + Vite 5173，不冲突）

### 9.2 首次上线配置（已完成）

1. **AUTH_SECRET**：已生成随机密钥写入服务器 `backend/.env`（防 token 伪造）
2. **公网访问**：nginx 80 端口反代到 8000（安全组已放行 80，无需额外配置）
3. 组员在任何网络（手机流量也行）打开 <http://8.166.134.107/> 即可

### 9.3 前端更新后（发布新版本）

在**本地电脑**执行（构建后上传服务器）：

```bash
cd frontend && npm run build
tar czf pt.tar.gz --exclude='*.map' -C frontend dist
scp pt.tar.gz root@8.166.134.107:/opt/ai4se-papertracker/
ssh root@8.166.134.107 "cd /opt/ai4se-papertracker && rm -rf frontend/dist && tar xzf pt.tar.gz && systemctl restart papertracker"
```

### 9.4 数据维护

**服务器上执行**（先 `ssh root@8.166.134.107`，然后在 `/opt/ai4se-papertracker/backend` 下）：

| 任务 | 命令 |
|---|---|
| 手动跑一次完整爬取 | `.venv/bin/python -m scripts.run_crawl` |
| 批量重新分类 | `.venv/bin/python -m scripts.run_classify` |
| 回填引用数 | `.venv/bin/python -m scripts.run_citations` |
| 复核 DBLP 匹配歧义 | `.venv/bin/python -m scripts.review_pending list` |
| 生成周报 | `.venv/bin/python -m scripts.weekly_report`（个人周报 `personal_report`） |
| 数据库迁移 | `.venv/bin/python -m alembic upgrade head` |

- 数据库与缓存位于 `/opt/ai4se-papertracker/data/`（SQLite：`papers.db`；LLM 成本记录 `llm_cost.json`；引用缓存 `citation_cache/`）
- **备份**：定期拷贝 `data/` 目录即可（论文数据 + 账号 + 收藏全在其中）
- 定时任务：每日爬取（北京 09:30）、每周五周报（系统 + 个人）由服务器后端调度器自动执行，无需干预

### 9.5 成本控制

- DeepSeek LLM（分类/摘要/亮点/深度摘要）：月度成本上限 `LLM_COST_LIMIT_USD`（默认 $5），超出自动暂停
- 引用数接口（Crossref / Semantic Scholar）：免费
- arXiv / DBLP：免费

---

## 10. 常见问题

| 问题 | 解决 |
|---|---|
| 打开地址是空白/连不上 | 确认地址为 <http://8.166.134.107/>；服务器宕机时联系管理员重启（`systemctl restart papertracker`） |
| 提示登录过期 | 重新登录（token 30 天有效） |
| 收藏/标记点了没反应 | 需先登录（未登录会引导到登录页） |
| 列表「被引」显示 — | 该论文未查到引用数（无 DOI 且 Semantic Scholar 无记录） |
| AI 深度摘要生成失败 | 稍后重试；检查 DeepSeek 成本上限是否已超 |
| 数据要备份 | 拷贝 `data/` 目录 |
| 想删除账号 | 联系管理员从 `users` 表删除 |

---

## 附：数据来源与合规

- **arXiv**：每日拉取 `cs.SE` 最新论文元数据与摘要（只展示，不下载存储 PDF）
- **DBLP**：判定 CCF-A 会议发表情况（预印本与正式版同一记录，双链接展示）
- **DeepSeek**：AI4SE 识别、主题标签、中文摘要、亮点速读、深度摘要
- **Crossref / Semantic Scholar**：被引次数
- 只展示公开元数据与 LLM 生成内容，不搬运付费墙内容
