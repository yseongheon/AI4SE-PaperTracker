# 交接文档 —— 2026-08-23 待办改动

> 写于 2026-08-22。今天只提交了「个人画像已读」功能并推送 GitHub，**下面的 3 项改动明天做，今天不碰代码**。

## 〇、当前状态速览（今天已完成）

| 项 | 说明 |
|---|---|
| 已读功能 | 个人画像新增「最近已读」列表；「✓ 已读」卡片可点击 → 论文列表页自动「只看已读」（`marks=read`）；FilterSidebar 加「只看已读 ✓」；列表页支持 `?marks=` 查询参数 |
| 后端接口 | `/api/papers`、`/api/export` 的 `marks` 参数由 `bookmark\|read_later\|unread` **开放为含 `read`** |
| 画像接口 | `/api/users/me/profile` 新增 `recent_read`（最近已读论文），`user_service._recent_marked()` 复用 |
| 测试 | 新增 `test_filter_read`、`test_profile_recent_read`；后端 192 pytest 全绿、前端 vue-tsc+build 通过 |
| Git | 已提交并推送到 GitHub（`main`，HTTPS remote 可用）；两个提交：`6cd4b26`（已读功能）、`24133a9`（M14 趋势图修复） |

**运行状态**：后端已在 8000 端口以 `--reload` 重启（旧进程曾因没加载新代码导致 `marks=read` 422）；dev 前端在 5173。浏览器 `localhost:5173` 刷新即可用。

> ⚠️ **重要坑（必读）**：`start.bat` 启动后端**不带 `--reload`**。改后端代码后必须重启 8000 进程，否则跑的还是旧代码、接口行为与源码不一致，极易误判成前端 bug。重启方式：`taskkill //PID <旧PID> //F`，再重新起，或改用 `uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload`。
> 另外：`frontend/dist` 已存在（今天 build 过），若重跑 `start.bat` 会进入**生产模式**（8000 单端口服务打包前端）而不是 dev（5173）；想回 dev 就删掉 `frontend/dist`。

---

## 明日 3 项改动

### 1. 删除「收藏论文主题分布」栏

**动机**：画像页顶部这张卡片信息量低、用户不需要。

改动点：
- `frontend/src/views/ProfileView.vue`
  - 删除「收藏论文主题分布」整张 `el-card`（约 127–144 行，含 `profile.topic_dist` 的 v-if/v-for 与条形样式绑定）。
  - 删除相关样式：`.topic-bars` / `.topic-bar` / `.topic-name` / `.bar-track` / `.bar-fill` / `.topic-count`。
- `frontend/src/types/index.ts`
  - `ProfileStats` 删除 `topic_dist` 字段（第 136 行）。
- `backend/app/services/user_service.py`
  - `profile()` 删除 `topic_dist` 的查询（`join Topic/PaperTopic` 那段）与返回键；若 `Topic`/`PaperTopic` 不再被引用则一并删 import。

⚠️ 注意：`scripts/personal_report.py` **也有一个 `topic_dist`**（个人周报用，独立逻辑），**不要动它**，否则个人周报会挂（测试 `test_personal_report.py` 会断言）。

### 2. 收藏论文也要有「查看全部」功能（与已读对齐）

**现状**：已读卡片可点击（`goReadList()` → reset + `marks='read'` + 跳 `/`），「最近已读」卡片头部有「查看全部 ›」。收藏目前只有静态数字和「最近收藏」列表（无查看全部、卡片不可点）。

改动点（全部在 `frontend/src/views/ProfileView.vue`）：
- 新增 `goBookmarkList()`，**仿照 `goReadList()`**：`filter.reset(); filter.marks = 'bookmark'; router.push('/')`。
- 「⭐ 收藏」统计卡片加 `@click="goBookmarkList"` + `class="clickable"`，label 改为「⭐ 收藏 ›」。
- 「最近收藏」`el-card` 的 `#header` 改为 `.card-head` flex 布局（同「最近已读」），右侧放「查看全部 ›」`el-link`。

后端无需改动（`marks=bookmark` 原本就支持）。

### 3. 把已读/收藏作为「高级筛选」的一种（解决进已读后列表恢复难）

**背景（用户原话要点）**：每次从画像页进「已读」，论文列表就被 `marks=read` 锁住，想恢复全部列表要刷新网页很麻烦。改成**高级筛选**后，列表页顶部的「已应用」摘要会显示条件，点「清除高级筛选」即可一键恢复。

**方案**：把阅读状态过滤从**侧栏**挪进**「高级筛选」弹窗**。侧栏不再显示阅读状态 → 侧栏恒「全部」；进已读/收藏由高级筛选承载，可被清除。

改动点：
- `frontend/src/components/FilterSidebar.vue`
  - 删除「阅读状态」整块（`marks` v-model 的 `el-radio-group`，含「只看收藏/已读/稍后读/未读」），并把 `storeToRefs` 里不再用的 `marks` 一并去掉。
- `frontend/src/views/PaperListView.vue`
  - 「高级筛选」`el-dialog` 内新增「阅读状态」区（radio：`全部 / 已读 ✓ / 收藏 ⭐`）。
  - `advSummary` computed 增加 marks 文案：`bookmark→收藏 / read→已读 / read_later→稍后读 / unread→未读`（这样进已读后顶部会显示「已应用：已读」）。
  - `clearAdv()` 的 `$patch` **必须加 `marks: ''`**（否则清除高级筛选清不掉阅读状态）。
- `ProfileView.vue`：`goReadList()` / `goBookmarkList()` 逻辑不变（仍设 `filter.marks`），跳转后自动体现在高级筛选摘要里。
- `frontend/src/api/papers.ts`：`exportUrl` 已透传 `marks`，无需改。

**待拍板小点**：「稍后读」「未读」是否也一起挪进高级筛选？
- 推荐**一起挪**：侧栏与高级筛选两处都能设 marks 会造成混乱，且统一后交互一致。
- 若只挪已读+收藏，侧栏可保留稍后读/未读，但需注意两处状态同步问题。

---

## 验证清单（改完后）

1. 后端：`cd backend && ./.venv/Scripts/python.exe -m pytest` 全绿（关注 `test_marks_api.py` 的 `test_filter_read`、`test_profile_recent_read`）。
2. 前端：`cd frontend && npm run build`（vue-tsc 严格模式 + vite build）通过。
3. 冒烟：
   - 画像页不再有「收藏论文主题分布」卡；
   - 「⭐ 收藏」卡片可点击、最近收藏头部有「查看全部 ›」→ 列表自动「只看收藏」；
   - 从画像进「已读」→ 列表顶部「已应用：已读」→ 点「清除高级筛选」→ **无需刷新**恢复全部列表。
4. **改完后端代码记得重启 8000 进程**（见顶部「重要坑」）。

## Git 记录（本次已推送）

```
24133a9 fix(M14): 会议×主题交叉柱状图改分组对比 + 画布外主题标签筛选；修复 ECharts legend selected 恢复失效/崩溃
6cd4b26 feat: 个人画像新增「最近已读」列表 + 已读卡片可点击跳列表页（marks=read）
```
