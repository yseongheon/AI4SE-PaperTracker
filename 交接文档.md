# AI4SE PaperTracker — 开发交接文档

> 给接手的开发者：本文件帮你从零在本机跑起、改功能、测试、部署。
> 详细设计与**全部决策记录**见同目录 `CLAUDE.md`（项目唯一事实来源，改东西先看它）；本文是速查上手版。

---

## 1. 项目名称与简介

**AI4SE PaperTracker**：自动跟踪「AI 赋能软件工程（AI4SE）」论文的网站。

- 每日从 **arXiv** 拉取 `cs.SE` 最新论文
- 用 **DBLP** 判定论文是否发表在 CCF-A 软件工程四大会议（ICSE/FSE/ASE/ISSTA）
- 用**关键词初筛 + DeepSeek LLM 精标**识别 AI4SE 论文，打主题标签、生成中文摘要
- Web 页面展示：论文列表 / 详情 / 趋势图表 / 词云 / 作者榜 / **机构榜** / 合作网络等
- 已部署：阿里云 ECS，公网 <http://8.166.134.107/>

## 2. 技术栈

| 层 | 技术 |
|---|---|
| 前端 | Vue 3 + TypeScript（strict）+ Vite + Element Plus + ECharts；Pinia / Vue Router / Axios |
| 后端 | Python **≥3.14** + FastAPI + SQLAlchemy 2.x + SQLite + APScheduler + Alembic；uv 管理依赖 |
| 外部服务 | arXiv API（爬取）、DBLP API（A 会判定）、DeepSeek API（分类/中文摘要/亮点）、Crossref + Semantic Scholar（引用数） |
| 包管理 | 后端 uv（`python -m uv run ...`）；前端 npm（国内 npmmirror 镜像） |

## 3. 目录结构

```
AI4SE-PaperTracker/
├── CLAUDE.md              # 开发文档 + 决策记录（唯一事实来源，改东西先看）
├── README.md              # 访客版说明
├── 交接文档.md             # 本文档
├── .env.example           # 环境变量模板（复制为 backend/.env 填密钥）
├── backend/
│   ├── pyproject.toml     # 依赖 + uv 配置
│   ├── alembic/           # 数据库迁移（版本链，最新 rev 接 8e07230e0ec0 之后）
│   ├── app/
│   │   ├── main.py        # FastAPI 入口（挂载 scheduler）
│   │   ├── config.py      # pydantic-settings 读 backend/.env
│   │   ├── db.py          # engine / SessionLocal
│   │   ├── models/        # ORM：paper / author / venue / topic / crawl_run / user / mark...
│   │   ├── schemas/       # Pydantic 请求/响应
│   │   ├── api/           # 路由：papers / stats / topics / venues / export / auth / users
│   │   ├── services/      # 业务层：paper_service / stats_service / export_service / auth_service
│   │   └── crawler/       # arxiv_client / dblp_client / matcher / classifier / pipeline / normalize / http_client / citation_client
│   ├── scripts/           # run_crawl / run_classify / run_citations / run_backfill_affiliations / weekly_report / seed_* / review_pending
│   └── tests/             # pytest（159 个，离线可跑）
├── frontend/
│   └── src/
│       ├── api/           # axios 封装（http.ts + 按资源分文件）
│       ├── stores/        # filterStore（筛选条件）/ paperStore（列表数据）/ authStore
│       ├── views/         # PaperListView / PaperDetailView / TrendView / LoginView / ProfileView
│       ├── components/    # FilterSidebar / TrendChart / ChartHeatmap / TopicTag
│       ├── types/         # 与后端 Pydantic schema 对齐的 TS 接口（改接口两边同步）
│       └── router/
└── data/                  # SQLite 数据库 + 各缓存（不入 git）
```

## 4. 快速开始（本地开发）

### 环境要求
- Python ≥3.14（uv 自动管理），Node ≥20

### 启动（两条命令，别用 start.bat 混一起调试）
```bash
# 后端（端口 8000；首次先复制 .env）
cd backend
cp ../.env.example ../.env    # 改到 backend/.env，填 DEEPSEEK_API_KEY
python -m uv run uvicorn app.main:app --reload --port 8000
# 或 ./.venv/Scripts/python.exe -m uvicorn app.main:app --reload --port 8000

# 前端（端口 5173；Vite 代理 /api → 127.0.0.1:8000）
cd frontend
npm install
npm run dev
```
浏览器访问 **http://localhost:5173**（注意用 localhost，别用 127.0.0.1——Vite 默认绑 IPv6 ::1）。
API 文档（Swagger）：<http://127.0.0.1:8000/docs>

### 初始化数据库（仅首次 / 新机器）
```bash
cd backend
python -m uv run alembic upgrade head            # 建表（8 张 + 后续列）
python -m uv run python -m scripts.seed_venues   # CCF-A 会议名单
python -m uv run python -m scripts.seed_topics   # 主题分类法
python -m uv run python -m scripts.run_crawl --backfill --days 180  # 回填论文（DBLP 限流，很慢，见坑）
python -m uv run python -m scripts.run_classify  # 关键词初筛 + LLM 分类 + 中文摘要
```
> 普通使用者/接手者**不要**重跑以上命令；数据由服务器统一维护。本地开发若已有 `data/papers.db` 直接用即可。

### 常用命令速查（cd backend 下）
| 任务 | 命令 |
|---|---|
| 跑一次爬取 | `python -m scripts.run_crawl` |
| 历史回填 | `python -m scripts.run_crawl --backfill --days N` |
| 批量分类 | `python -m scripts.run_classify`（`--dry-run` 估算、`--limit 20` 试跑） |
| 机构回填 | `python -m scripts.run_backfill_affiliations`（`--limit 50` 试跑、`--force` 覆盖） |
| 引用数回填 | `python -m scripts.run_citations` |
| 数据库迁移 | `python -m uv run alembic upgrade head` |
| 跑测试 | `python -m uv run pytest` |

## 5. 架构与数据管线

```
APScheduler 每日定时（09:30）→ 手动 CLI 兜底
 ① arXiv 拉取（cs.SE，≥3s/请求）→ ② 归一化去重 upsert（arxiv_id 幂等）
 ③ DBLP A 会匹配（会议流批量拉取，本地标题+作者匹配）→ ④ 关键词初筛
 ⑤ DeepSeek LLM 精标（AI4SE 判定 + 主题 + 中文摘要 + 亮点）→ ⑥ crawl_runs 审计
 ⑦ FastAPI REST API → ⑧ Vue 3 前端展示
```

**关键表**（详见 CLAUDE.md 第 5 章）：
- `papers`：核心表，arxiv_id / dblp_key 唯一、双链接展示、AI4SE 标记、中文摘要、引用数
- `paper_authors`：N:M 关联，含 `affiliation` 列（M11 机构，arXiv 每作者机构规则归一化）
- `venues` / `topics`：CCF 名单 / 主题分类法，都是**数据不是代码**（增删改数据即可）
- `users` / `user_marks`：课题组账号 + 个性化标记（复合主键，按用户隔离）
- `crawl_runs`：每次运行审计日志
- `keyword_rules`：关键词初筛规则（可配置）

**前后端接口约定**（列表响应 `{items, total, page, page_size}`，错误 `{detail}`）。
主要端点：`GET /api/papers`（大量过滤参数）、`/api/papers/{id}`、`/api/stats/trends|authors|institutions|words|cross|coauthor`、`/api/export`、`/api/auth/*`。完整见 CLAUDE.md 第 9 章。

## 6. 硬性约束（红线，违反会被封 IP / 超预算）

| 约束 | 规则 |
|---|---|
| **决策规则** | 任何技术方案选择必须先列 **≥2 个候选**并解释优劣，由用户拍板；拍板写入 CLAUDE.md 决策表。**不得自行选定** |
| arXiv API | 每请求间隔 **≥3s**、单连接、max_results ≤500；指数退避重试 |
| DBLP API | 每请求 **≥2s**；突发 >3~6 请求/分钟即封（429/连接重置）；客户端已实现「失败等 90s 重试、每 5 页休 20s、全量结果 7 天缓存 data/dblp_cache/」。**单次匹配前先估算请求数，不要连续重复跑匹配** |
| DeepSeek API | 并发 **2**、失败重试最多 2 次；月度成本上限 `LLM_COST_LIMIT_USD`（默认 $5），超出自动暂停 |
| 数据合规 | 只展示论文元数据 + 公开链接 + LLM 生成摘要，不下载存储 PDF、不搬运付费墙内容 |
| 代码规范 | 后端 api→services→models 分层；所有表结构变更走 Alembic 迁移；前端 vue-tsc strict + 组件命名 PascalCase；改接口时前后端类型同步 |

## 7. 测试

- 后端：`cd backend && python -m uv run pytest` —— **159 个测试全绿**，全部离线（内存 SQLite + 固定 fixture，不碰真实网络）
- 覆盖重点：标题归一化、DBLP 匹配与歧义、限流退避、爬虫解析（fixture XML）、分类/摘要、API 集成、导出、周报、机构榜计数、认证
- 前端：`cd frontend && npx vue-tsc --noEmit`（严格类型）+ `npm run build`
- **新增功能后**：pytest 全绿 + vue-tsc/build 通过才算完成（项目惯例）

## 8. 关键决策记录摘要（完整 29 条见 CLAUDE.md 第 2 章）

| 编号 | 决策 |
|---|---|
| DR-001~008 | FastAPI / arXiv+DBLP / SE 四大 A 会 / 关键词+LLM 混合 / DeepSeek / SQLite / Element Plus / APScheduler |
| DR-010 | GitHub 推送走 SSH over 443（`git@ssh.github.com`），绕过 HTTPS 代理问题 |
| DR-015 | DBLP 用**会议流批量拉取**（不是逐篇搜索），请求数极少、命中率高 |
| DR-018 | LLM 精标只标关键词初筛候选（成本控制） |
| DR-020 | 趋势前端聚合（后端按天返回，周/月前端算） |
| DR-026 | 引用数：Crossref(DOI) + Semantic Scholar(arXiv) 双源（OpenAlex 被墙废弃） |
| DR-027 | 认证：自建 HMAC token + pbkdf2（零新依赖） |
| DR-029 | 机构功能：arXiv affiliation + `paper_authors.affiliation` 字符串列 + 规则归一化 + 作者榜同页并排 |

## 9. 当前状态（2026-08-20）

- **里程碑**：M0~M10 完成（M10 = 阿里云生产部署）。M11 机构功能完成（本机）。
- **本地数据库**：3901 篇论文、2283 篇 AI4SE、A 会匹配 62 篇、机构 91 篇有数据（140 个原始机构 → 过滤后几十个真实机构）
- **待办/已知限制**：
  - Semantic Scholar API（`api.semanticscholar.org`）当前被墙/限流，引用数只回填了 94 篇（有 DOI 的），纯 arXiv 论文引用数空缺
  - arXiv 机构覆盖仅 ~2%，机构榜偏小；S2 通了可扩
  - 生产服务器（阿里云 8.166.134.107）**尚未**同步 M11 机构改动（迁移 + 代码 + 回填需在服务器跑一次）
- **未完成功能**：无明确挂起项；看用户后续需求

## 10. 运维与部署（M10 已上线，改动后要同步）

- 生产地址：<http://8.166.134.107/>（nginx 80 反代 → 127.0.0.1:8000，systemd `papertracker.service`）
- 代码位置：服务器 `/opt/ai4se-papertracker/`
- **更新流程**：本地 `cd frontend && npm run build` → `tar czf pt.tar.gz -C frontend dist` → scp 到服务器 → `ssh root@8.166.134.107 "cd /opt/ai4se-papertracker && rm -rf frontend/dist && tar xzf pt.tar.gz && systemctl restart papertracker"`
- **涉及数据库变更**时：还需 scp 后端代码（backend/ + 迁移）上去，服务器上跑 `alembic upgrade head` + 对应回填脚本
- 运维：`systemctl status papertracker`、`tail -f /root/citations.log`、备份 = 拷 `/opt/ai4se-papertracker/data/`
- SSH：root@8.166.134.107，需本机配好 `~/.ssh/id_ed25519`（当前开发机未配，拉库/部署前先解决）

## 11. 已知坑与注意事项（都是实测踩过的）

1. **uvicorn `--reload` 在 Windows 上不可靠**：改后端代码可能不触发重载（本机实测只重载了一次）。改完后**手动重启后端**，别依赖热重载。
2. **DBLP 翻页参数是 `f`（first）不是 `start`**：用 `start` 会每页返回相同 100 条导致无限循环（客户端已修正）。**stream key 与 DBLP 集合 key 不同**：FSE→`conf/sigsoft`、ASE→`conf/kbse`。
3. **DBLP 全历史拉取极慢**：ICSE 7894 条 = 79 页，首次全量可能 30 分钟+（限流 429 自动等 90s）。拉完 7 天缓存，之后秒读。**一次只跑一个爬取进程**，别并发抢 DBLP 额度。
4. **arXiv `id_list` 必须显式传 `max_results`**：默认只返回 10 条（机构回填脚本已处理）。
5. **`run_classify` 重复执行**：关键词初筛会给已分类论文重插 keyword 标签撞唯一约束。**已修复**（keyword_rules.py 跳过 status=classified 的论文）。别回退。
6. **机构归一化正则顺序**：先匹配"带句点缩写"（吞掉 `Univ.` 的句点）再匹配裸缩写，否则 `Univ.,` 后跟逗号时句点残留（normalize.py 已处理）。
7. **Git 代理**：本机全局 git 配了 `http://127.0.0.1:7892`（Clash），连不上 GitHub 时检查代理是否在跑。
8. **控制台中文乱码**：Git Bash 下 Python 日志中文可能显示乱码，是编码显示问题，数据本身是 UTF-8 正常的。
9. **S2/OpenAlex 被墙**：`api.semanticscholar.org`、`api.openalex.org` 国内直连不通（Cloudflare 被拦截），走代理可能限流。需要它们的功能要先解决网络。
10. **SQLAlchemy `Query.get()` 弃用警告**：无害，存量代码在用。

## 12. 接手开工清单

1. 读完本文 + `CLAUDE.md`（尤其第 2 章决策规则、第 5 章关联规则、第 6 章限流红线）
2. 配好 `backend/.env`（DEEPSEEK_API_KEY），起后端 + 前端
3. 跑一遍 `pytest` 确认 159 全绿
4. 需要真实数据时：确认本地 `data/papers.db` 是否已带数据；没有就从服务器 scp 或本地回填
5. 改功能遵循：先列方案给用户拍板 → 实现 → 迁移（如需）→ 补测试 → pytest + vue-tsc/build 全绿 → 通知用户浏览器验收
6. 改动要上线时：按第 10 节流程同步到阿里云（含迁移和回填脚本）
